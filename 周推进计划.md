# 第4周周推进计划

小组：第 5 组　　周次：第 4 周　　组长：刘彦伯　　填表日期：2026 年 9 月 24 日

## 1. 上周计划兑现情况

- 已完成安全隧道的基础代码：后端已增加 `app/utils/tunnel.py` 和 `app/middleware/secure_tunnel.py`，前端已增加 `src/utils/sm-tunnel.js`，并在 Axios 请求、响应拦截器中接入。
- 已在 `/user/login` 接口上完成安全隧道联调入口。当前代码能够为每次请求生成随机会话密钥、IV、Nonce 和时间戳，使用 SM2 封装会话密钥，并完成时间窗校验和 Nonce 防重放。
- 后端隧道自测已通过 SM4 加解密、SM2 信封、完整报文往返、重复请求拦截、过期请求拦截和非法报文拦截；后端源码语法检查通过。

## 2. 本周目标

本周对应任务书“第4周：应用层安全隧道 Demo”节点，目标是在现有隧道代码上完成可验收闭环：

1. 继续使用 Axios 拦截器和 Flask 装饰器统一处理敏感 JSON，先保证 `/user/login` 全链路稳定，再根据联调情况覆盖注册、后台登录或下单等敏感接口。
2. 完成验收要求的 6 类验证：正常加解密、每次会话密钥与 Nonce 不同、密文篡改被拒绝、重复请求被拒绝、过期请求被拒绝、抓包中不出现用户名和密码明文。
3. 将协议、代码、测试步骤和原始证据提交到仓库，保证老师可以按文档复现。

## 3. 分工

| 姓名 | 负责模块 | 本周交付物 |
| --- | --- | --- |
| 刘彦伯 | 后端密码模块与安全隧道：完成认证标签校验、SM2 会话密钥封装、时间窗和 Nonce 防重放 | `后端flask/app/utils/SM4.py`、`后端flask/app/utils/tunnel.py`、`后端flask/app/middleware/secure_tunnel.py` |
| 贺正义 | 前端加密与接口接入：调整报文格式，在 Axios 拦截器中统一完成敏感 JSON 加密、响应解密和隧道错误处理 | `v1-zhognshe-rebuild-3-vue/src/utils/sm-tunnel.js`、`v1-zhognshe-rebuild-3-vue/src/request.js`及相关接口配置 |
| 孙楠 | 核对证书、跨域和环境变量，完成真实登录流程并检查每次会话密钥与 Nonce 的随机性、验收材料、编写报告文档 | `后端flask/run.py`、前端环境配置、联调记录和启动说明 |
| 刘海翔 | 测试、抓包：编写正常、篡改、重放、过期和非法报文测试，整理抓包样例及失败场景记录 | `后端flask/tests/test_tunnel.py`、`wireshark抓包样例，隧道成功失败场景记录 |

## 4. 本周预期交付物

- `后端flask/app/utils/SM4.py`、`后端flask/app/utils/tunnel.py`：SM4-GCM 加解密、认证标签校验、随机会话密钥、时间窗和防重放实现。
- `后端flask/app/middleware/secure_tunnel.py`：统一请求解密与响应加密中间件。
- `v1-zhognshe-rebuild-3-vue/src/utils/sm-tunnel.js`、`v1-zhognshe-rebuild-3-vue/src/request.js`：Axios 统一加密、解密和错误处理。
- `后端flask/tests/test_tunnel.py`：正常、篡改、重放、过期、非法报文和随机性测试。
- `后端flask/docs/第4周验收/抓包样例.md`：记录抓包环境、请求截图或 HAR/原始报文，并标注敏感 JSON 未以明文出现。
- `后端flask/docs/第4周验收/失败场景测试记录.md`：记录密文篡改、重复请求和过期时间戳的请求、响应、状态码与结论。
- `README.md`：补充前后端 HTTPS 启动方式、环境变量、演示步骤和验收复现顺序。

完成标准：上述交付物全部入库；HTTPS 保持启用；前端和后端能够完成一次真实登录；篡改、重放和过期请求均被明确拒绝；抓包中看不到用户名、密码等敏感 JSON 明文。
